{
  "name": "uprtcl-tests",
  "scripts": {
    "test": "truffle test",
    "migrate": "truffle migrate",
    "dev": "concurrently \"ganache-cli --networkId 3333 --blockTime 1 --deterministic\" \"npm run migrate\" "
  },
  "dependencies": {
    "@types/node": "^12.0.8",
    "buffer": "^5.2.1",
    "cids": "^0.7.1",
    "dotenv": "^8.0.0",
    "multihashing-async": "^0.7.0",
    "truffle": "^5.0.22",
    "truffle-privatekey-provider": "^1.3.0",
    "typedarray-to-buffer": "^3.1.5"
  },
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}
